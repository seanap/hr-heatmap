services:
  hr-heatmap:
    build: .
    container_name: hr-heatmap
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data/cache:/data/cache
      - ./data/output:/data/output
    ports:
      - "8246:8000"
